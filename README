EE: Publish Checklist:
Ensure logged into npm: npm login

1. Update package.json version number
2. yarn format && yarn lint && yarn prepublishOnly
3. git add . && git commit -m ''
4. git push
4. Publish npm & yarn: $ npm publish
5. Ensure Gihub Actions & all badges shows pass